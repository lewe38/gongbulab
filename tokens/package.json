{
  "name": "@gongbulab/tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Design tokens — source of truth for visual identity across web and iOS",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {}
}
